#include "Template Functions.h"
#include "Rational.h"

/*int main()
{
	/// example of int ///
	int arr1[] = {1,8,9,7};
	cout<<"Max value is "<<MaxValue(arr1,4)<<endl;
	if(ExistValue<int>(8,arr1,4)) // example of found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;;
	if(ExistValue<int>(2,arr1,4)) // example of not found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;
	 cout<<endl;

	/// example of float ///
	float arr2[] = {1.8 , 8.4 , 9.9 , 7.5};
	cout<<"Max value is "<<MaxValue(arr2,4)<<endl;
	if(ExistValue<float>(7.5,arr2,4)) // example of found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;
	if(ExistValue<float>(2.6,arr2,4)) // example of not found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;
	 cout<<endl;

	/// example of string ///
	string arr3[] = {"hello","bye"};
	cout<<"Max value is "<<MaxValue(arr3,2)<<endl;
	if(ExistValue<string>("bye",arr3,2)) // example of found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;
		if(ExistValue<string>("no",arr3,2)) // example of not found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;
	cout<<endl;

	/// example of char ///
	char arr4[] = {'a', 'C','E','z','z'};
	cout<<"Max value is "<<MaxValue(arr4,5)<<endl;
	if(ExistValue<char>('C',arr4,5)) // example of found
		cout<<"value exist";
	else
		cout<<"value doesn't exist"<<endl;
	if(ExistValue<char>('t',arr4,5)) // example of not found
		cout<<"value exist";
	else
		cout<<"value doesn't exist"<<endl;
	 cout<<endl;

	/// example of class Rational ///
	Rational arr5[] = {Rational(3,5) , Rational(1,2), Rational(3,4) };
	cout<<"Max value is "<<MaxValue(arr4,3)<<endl;
	if(ExistValue<Rational>(Rational(1,2),arr5,5)) // example of found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;
	if(ExistValue<Rational>(Rational(8,8),arr5,3)) // example of not found
		cout<<"value exist"<<endl;
	else
		cout<<"value doesn't exist"<<endl;
	 cout<<endl;

return 0;
}*/